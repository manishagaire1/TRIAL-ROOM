// The clothing catalog now comes from the real backend (see api/clothing.ts
// and app/seed.py) — this file only holds placeholder data for things that
// still don't have a real backend source yet (trial history in Phase 7+).

export const sampleDashboardStats = {
  profileCompletionPercent: 40,
  recentTryOnsCount: 0,
  savedOutfitsCount: 0,
  shoppingListCount: 0,
}
